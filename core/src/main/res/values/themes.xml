<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DalilMuslim" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="BottomNavigationTheme" parent="Theme.MaterialComponents.DayNight">
        <item name="colorPrimary">@color/white</item>
    </style>

    <style name="shapeImage" parent="">
        <item name="cornerSize">20%</item>
    </style>

    <style name="MySearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!--  Gets rid of the search icon  -->
        <!--  Gets rid of the "underline" in the text  -->
        <item name="queryBackground">@null</item>
        <item name="android:fontFamily">@font/quran</item>
        <!--  Gets rid of the search icon when the SearchMainView is expanded  -->
        <item name="searchHintIcon">@null</item>
        <item name="android:editTextColor">@android:color/white</item>
        <!--         <item name="android:editTextColor">@android:color/white</item> -->
        <!--  The hint text that appears when the user has not typed anything  -->
        <item name="queryHint">@string/search_hint</item>
        <!-- <item name="closeIcon">@drawable/ic_close</item> -->
        <!--         <item name="android:textColorHint">@android:color/white</item> -->
        <!--         <item name="android:textColor">@android:color/white</item> -->
        <item name="android:textSize">15sp</item>
    </style>

    <string name="search_hint">Search ....</string>

    <style name="WrapContentDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="windowMinWidthMajor">0%</item>
        <item name="windowMinWidthMinor">0%</item>
    </style>
</resources>